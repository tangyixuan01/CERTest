//c:char;d:int;e:unsigned;a:int;g:int
while (d >= 0)
{
  unsigned short i = (~(-((e ^ 10375) ^ (~c)))) ^ 10159;
  g = e;
  a = c - (((a | (~e)) & (-i)) & ((~d) | e));
  d = ~((i & a) * e);
  if (d > (-416522375))
    continue;
  return;
}
