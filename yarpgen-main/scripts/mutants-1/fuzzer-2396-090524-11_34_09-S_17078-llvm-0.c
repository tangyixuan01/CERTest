#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-if-snippet

int printf(const char *, ...);
int a;
int b = 1;
int c;
int d;
int e;
int f;
int bar(int p)
{
  return ((p > 0) && (1 / p)) ? (1) : (p);
}

int baz(int *u)
{
  unsigned leE93uiY = 3291293766U;
  unsigned soW7cM2E = 0U;
  int g;
  int *h;
  transparent_crc(soW7cM2E, "soW7cM2E", 1);
  transparent_crc(leE93uiY, "leE93uiY", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  for (;; ++c)
  {
    int i;
    int **j = &h;
    if (c)
    {
      for (b = 0; b < 1; b = 1)
      {
        if (a | bar(6 | f))
        {
          return 0;
        }
        if (soW7cM2E > leE93uiY)
        {
          __builtin_abort();
        }
      }

    }
    else
    {
      *j = &i;
    }
    *u = -4;
    *h = d & (-8);
    g = (*(*j)) + (*(*j));
    if ((*u) >= g)
    {
      return 0;
    }
  }

}

int main()
{
  platform_main_begin();
  crc32_gentab();
  baz(&e);
  printf("%d\n", b);
  return 0;
}

