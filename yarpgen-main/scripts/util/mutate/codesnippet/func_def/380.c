//i:int;a7:char;d8:short;d7:short;s:short;x:int;j:int;k:int;ac:int;z:int;an:int;f:char;u:char;ad:int;r:short
static char fn3()
{
  short w;
  for (i = 0; i < 8; i++)
  {
    char ap = (a7 % ac) + d8;
    unsigned aq = (~0x1F) & (u % ((7 * (~ap)) ^ ad));
    if ((aq < 1027) && z)
      a7 = ((1958 | f) + 15) & 15;
  }

  for (; s; s++)
  {
    short as = 0;
    for (; as < 3; as++)
      for (d7 = 2; d7 >= 0; d7--)
      if (an)
      for (; an >= 0; an--)
      u = x;

    else
    {
      short at = an = as;
      if (at <= j)
        w = (k) ? (r) : (k);
      if (w)
        return s;
    }


  }

  return 0;
}

