#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-while-snippet

int printf(const char *, ...);
int a;
int b;
int c;
int d;
int e;
int f;
char g(int h, int i)
{
  return (i > 1) ? (h) : (h >> i);
}

void j()
{
  for (; b < 1; b++)
  {
    int k = 54;
    for (e = 0; e < 7; e++)
    {
      for (; a < 2; a++)
      {
        ;
      }

    }

    for (d = 0; d < 1; d++)
    {
      if (g(3, k))
      {
        return;
      }
    }

    transparent_crc(k, "k", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  }

  f--;
}

int main()
{
  platform_main_begin();
  crc32_gentab();
  int wCZx9WSH = -759285109;
  j();
  printf("%d\n", f);
  while (1)
  {
    int m = wCZx9WSH;
    while (m)
      m /= 2;

    break;
  }

  transparent_crc(wCZx9WSH, "wCZx9WSH", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  return 0;
}

