//h:int;d:unsigned;f:int;e:short;c:char;b:int;i:char;a:int
for (; a >= 0; a--)
{
  int *j = &h;
  char k = i || c;
  short l = ~h;
  b = ~(d % h);
  f = (~i) / b;
  if (h >= (-1936967575))
  {
    int m = ((h / d) / 110000000) ^ (f & c);
    short n = c;
    if (h <= (-1936967575))
      n = e;
    h = m;
    c = ~(n + f);
    d = -1;
  }
  i = (~l) - k;
}
