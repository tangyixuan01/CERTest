//h:int;d:int;f:short;a:volatile short;k:int;e:char;c:int;n:unsigned short;b:char;g:volatile short;m:int;l:volatile short;i:int;j:short
for (;;)
{
  int o = ~((e && f) && j);
  int p = ~((~((h % (~k)) ^ e)) & b);
  char q = (~(b | e)) ^ ((p && (o ^ j)) >> (~i));
  short r = ((n & h) && q) | (~(o | (e | p)));
  short s = (((h && q) && j) && (i % l)) * e;
  if (e)
    k = r | b;
  j = s;
  if (!(c >= 0))
  {
    h++;
    printf("g_5326=%lld\n", (long long) 2);
  }
  (a / b) && a;
  short t = ~g;
  n = t;
  L1:
  if (l)
  {
    printf("l_5906=%lld\n", (long long) l);
    goto L2;
  }

  continue;
  L2:
  if (d)
    goto L1;

  return m;
}
