//c:unsigned int;f:int;a:int
if (f <= a)
{
  int g = -(a && 1);
  int h = (g - f) && a;
  int i = (~(c / f)) && (1 % (a | h));
  if (c)
  {
    g = f;
    if (i || (g && (g > (-465984012))))
      goto L2;
  }
  c = g | (f / c);
}
