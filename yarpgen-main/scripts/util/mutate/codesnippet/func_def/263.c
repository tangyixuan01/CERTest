//b:int;a:unsigned int;res:unsigned long long int
int main()
{
  res = (unsigned long long int) ((1 < ((b) ? (a) : (1558001202))) ? (1) : ((b) ? (a) : (1558001202)));
  printf("%llu\n", res);
  return 0;
}

