//ag:long;h:int;al:volatile unsigned;a:unsigned;d:signed;q:int;c:long;ae:int;l:long long;m:long;ad:int;ak:signed;ah:long;printf:func int;ai:char
if (m || q)
{
  bh:
  l = (ad = c);

  int bm = al || q;
  al;
  al;
  char bn = al || q;
  al;
  al;
  bm = q;
  ae = a;
  bk:
  ai = h || (q > d);

  ag = d;
  al;
  al;
  printf("%d", q);
  if (a > 1)
    break;
  if (q)
    printf("%d", d);
  if (q)
  {
    printf("3");
    h = d | (bm > q);
    goto bk;
  }
  if ((!ai) || (al && 0))
  {
    printf("%d", d);
    al;
    printf("%d", a);
    goto bb;
  }
  d = al;
  printf("%lld", l);
  m = q;
  if (ak)
  {
    printf("%ld", c);
    ad = c & q;
  }
  if (!ah)
  {
    printf("%d", q);
    goto bh;
  }
}
