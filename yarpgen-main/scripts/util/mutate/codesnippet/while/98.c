//c:char;b:char;a:char;d:unsigned char
while (1)
{
  if (c >= a)
  {
    for (c = 0; c != (-4); c -= 3)
    {
      while (!d)
        b = 0;

      continue;
    }

  }
  d = ~a;
  if (!d)
    continue;
  return 0;
}
