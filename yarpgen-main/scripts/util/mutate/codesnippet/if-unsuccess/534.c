//h:unsigned;b:char;c:unsigned;g:short;printf:func int;f:unsigned
if (b >= 0)
{
  h = b;
  c = b / 290;
  f = ~(c - (8 || h));
  if (f)
    printf("%d\n", 1);
  if (f)
    printf("%d\n", f);
  g = ~f;
  if (c < 3)
  {
    int i = (-h) < (~c);
    unsigned j;
    if (i)
      j = h;
    h = (-j) * g;
  }
  c = h;
}
