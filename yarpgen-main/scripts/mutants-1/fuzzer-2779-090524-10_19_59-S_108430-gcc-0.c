#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-switch-snippet

static long d;
static int i = 37;
static unsigned long a[22];
static unsigned short c[22];
static unsigned g[80];
static unsigned short *h = c;
static unsigned long *j = a;
int main()
{
  platform_main_begin();
  crc32_gentab();
  int o5HtVF6A = -2147483648;
  for (long m = 0; m < 8; ++m)
  {
    d = 1;
  }

  for (unsigned char p = 0; p < 17; p += ((d) ? (i) : (0)) - 35)
  {
    long t = (h[p]) ? (i) : (j[p]);
    switch (o5HtVF6A)
      case 111:
      case 222:
      case 44:
      return 0;




    g[p] = t;
    transparent_crc(t, "t", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  }

  transparent_crc(o5HtVF6A, "o5HtVF6A", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  if (g[0])
  {
    __builtin_abort();
  }
}

