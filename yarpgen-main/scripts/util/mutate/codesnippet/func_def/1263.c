//e:int;d:int **;printf:func int;a:int;b:int;h:int;g:int;f:int
int main()
{
  char i;
  int j;
  for (; g < 2; g++)
  {
    e = 1;
    for (e = 0; e < 2; e++)
    {
      int *k = &f;
      h = (b == 0) ? (g) : (0);
      j = (h ^ a) ? (h) : (h - a);
      i = j * g;
      *k = i;
      *d = 0;
    }

  }

  printf("%d\n", f);
  return 0;
}

