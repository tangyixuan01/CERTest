#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-assignment-snippet

int printf(const char *, ...);
int a;
int b;
int c;
int d = 1;
int e;
int g;
int h;
short f;
int fn1(int p1, int p2)
{
  return ((-p2) < 0) ? (0) : (p2);
}

int fn2()
{
  return b = 0;
}

void fn3(int p)
{
  int i;
  int j = d = 0;
  i = (1) ? (1) : (0);
  f = ((j || j) || (0 > j)) ? (0) : (j);
  if (!((1 & i) && f))
  {
    if (g)
    {
      ;
    }
    for (g = 0; g != 1; g++)
    {
      g || fn1(fn2(), p);
      if (!d)
      {
        e = g || a;
        break;
      }
    }

    for (h = -1; c >= 0; c = h)
    {
      if (g)
      {
        break;
      }
    }

  }
}

int main()
{
  platform_main_begin();
  crc32_gentab();
  int yzuSMBCb = -2147483648;
  fn3(1);
  yzuSMBCb += 4;
  printf("%d\n", c);
  transparent_crc(yzuSMBCb, "yzuSMBCb", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  return 0;
}

