//e:char;c:int;d:int;a:int;b:unsigned int
int main()
{
  for (; c < 1; c++)
  {
    for (b = 0; b < 2; b++)
      a = d != 0;

    e = a - 8;
    if (((unsigned int) e) >= b)
      break;
  }

  return c;
}

