#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-switch-snippet

int a;
int b;
int c;
int d;
int e;
int f;
int fn1(int p)
{
  return (p < 0) ? (0) : (-p);
}

int fn2(long long p, int q)
{
  int wp_UvaGE = 1975630971;
  transparent_crc(wp_UvaGE, "wp_UvaGE", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  switch (wp_UvaGE)
    case 111:
    case 222:
    case 44:
    return 0;




  return ((p && q) && (p > q)) ? (0) : (p + 1);
}

int fn3(int p)
{
  for (;;)
  {
    e = (p == 0) ? (0) : (1 / p);
    for (c = 0; c < 1; c = a)
    {
      f = fn2(fn1(--e), p);
      if (b)
      {
        return 0;
      }
    }

  }

}

int main()
{
  platform_main_begin();
  crc32_gentab();
  b++;
  fn3(1 & d);
  return 0;
}

