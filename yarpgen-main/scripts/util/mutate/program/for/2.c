//rx:int  tj:const int  in:int  as:int  
for (rx = 0; rx < tj; ++rx)
{
  for (in = 0; in < tj; ++in)
    as[in] = 1;

  as[rx] = 0;
}
//rx:int  tj:const int  in:int  as:int  
for (rx = 0; rx < tj; ++rx)
{
  for (in = 0; in < tj; ++in)
    as[in] = 1;

  as[rx] = 0;
}
