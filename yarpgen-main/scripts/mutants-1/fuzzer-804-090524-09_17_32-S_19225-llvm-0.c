#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-for-snippet

int *a;
int b;
int c;
int e;
int **f = &a;
long long d = -1;
static int *fn1(long long p)
{
  int g;
  if (d < 0)
  {
    for (b = 1; b >= 0; b--)
    {
      int h = p;
      g = (h == 0) ? (0) : (2 % h);
      e = g || c;
      transparent_crc(h, "h", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
    }

  }
  return 0;
}

int main()
{
  platform_main_begin();
  crc32_gentab();
  int fru9DANM = -69125402;
  for (; fru9DANM < 2; fru9DANM++)
    g:
  ;


  fn1(a == 0);
  transparent_crc(fru9DANM, "fru9DANM", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  return 0;
}

