//e:char;i:long;l:int;c:int;d:short;g:int;j:int;n:int;k:int;printf:func int;a:long;m:int;f:int;h:char;b:long
int main()
{
  while (1)
  {
    L:
    for (; j < 10; j++)
    {
      for (h = 2; h >= 0; h--)
        while (1)
      {
        if (l)
          f = 2;
        int q = (((9L / g) / (~e)) | (n ^ c)) | f;
        int t = 9 & (-(((~e) / q) & g));
        char aa = (((~(((~i) * (-c)) & (~f))) / n) ^ t) ^ 9;
        long ab;
        if (d)
          break;
        if (e)
          ab = (i = (~(((e & (9L * (g * aa))) & (~c)) & f)) & n);
        e = (~(((n / (-i)) * e) & ab)) | (-((-aa) | g));
        i = (((~i) / (~e)) / q) ^ (~t);
        if (g)
        {
          printf("%d\n", e);
          goto L;
        }
        while (h > b)
          ;

        g = (c = e);
        break;
      }


      for (n = 0; n < 10; n++)
        if (a && (h < c))
        break;

      if (k)
      {
        printf("%d\n", k);
        continue;
      }
      f = h % ((unsigned) 9);
    }


    if (f > 3)
      m = 0;
    int s = 46744073709551608 % m;
    if (s)
      break;
  }

  return 0;
}

