//e:int;c:int;d:int *;printf:func int;a:int;f:int *;g:int
int main()
{
  int h = 0;
  for (; a < 2; a++)
  {
    int i;
    for (g = 0; g < 2; g++)
      if (a < h)
    {
      e = i % 2;
      c = *f;
    }

    for (h = 0; h < 3; h++)
    {
      if (d)
        break;
      i--;
      printf("0");
    }

  }

  return 0;
}

