//i:unsigned short *;o:func char;k:func long;j:unsigned;printf:func int;a:int;h:unsigned short;b:long
int main()
{
  int p;
  int q;
  for (; (j - 9) >= 29; j = k(-95, *i) + 127)
  {
    (*i) || o();
    long d = h;
    p = (d == 0) ? (0) : (b % d);
    q = p;
    int *r[1] = {&q};
  }

  printf("%d\n", a);
}

