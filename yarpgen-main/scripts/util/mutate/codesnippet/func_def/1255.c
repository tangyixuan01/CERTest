//e:int;c:int;d:int;a:int;fn2:func int;f:short;g:int;fn1:func int;h:int
void fn3(int p)
{
  int i;
  int j = d = 0;
  i = (1) ? (1) : (0);
  f = ((j || j) || (0 > j)) ? (0) : (j);
  if (!((1 & i) && f))
  {
    if (g)
      ;
    for (g = 0; g != 1; g++)
    {
      g || fn1(fn2(), p);
      if (!d)
      {
        e = g || a;
        break;
      }
    }

    for (h = -1; c >= 0; c = h)
      if (g)
      break;

  }
}

