//e:unsigned short;i:int;c:unsigned short;d:unsigned short;a:int;b:unsigned short;f:unsigned short;g:unsigned short;h:long long
void fn1()
{
  int j = 0;
  for (; a;)
    L1:
  ;


  unsigned char k = e;
  if (f)
  {
    L2:
    h = (b % h) & k;

    goto L1;
  }
  int l = j;
  int m = (~i) * d;
  j = (~(g || d)) ^ (d >> ((h && c) * m));
  e = (d || m) + d;
  d = (~(-(d && (m >> i)))) & (~(j && (l || h)));
  if (!(d < c))
    goto L2;
}

