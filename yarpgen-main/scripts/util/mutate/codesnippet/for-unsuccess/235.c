//h:int *;d:int;f:int;c:int;b:int;g:int;bar:func int;a:int
for (;; ++c)
{
  int i;
  int **j = &h;
  if (c)
    for (b = 0; b < 1; b = 1)
  {
    if (a | bar(6 | f))
      return 0;
  }

  else
    *j = &i;
  *u = -4;
  *h = d & (-8);
  g = (*(*j)) + (*(*j));
  if ((*u) >= g)
    return 0;
}
