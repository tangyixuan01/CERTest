#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-if-snippet

int printf(const char *, ...);
int a;
int b;
int e;
int f;
int g;
int h;
int *c;
int **d = &c;
int main()
{
  platform_main_begin();
  crc32_gentab();
  char i;
  int j;
  for (; g < 2; g++)
  {
    e = 1;
    for (e = 0; e < 2; e++)
    {
      int *k = &f;
      h = (b == 0) ? (g) : (0);
      j = (h ^ a) ? (h) : (h - a);
      i = j * g;
      *k = i;
      *d = 0;
      if (""['\0'])
      {
        #pragma message "Gotcha!"
        __builtin_abort();
      }
    }

  }

  printf("%d\n", f);
  return 0;
}

