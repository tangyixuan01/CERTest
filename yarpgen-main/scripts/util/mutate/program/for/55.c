//a:short  b:short  c:int  d:void  
for (; a <= 4; a++)
{
  short *f = &b;
  c[a] || (*f = 0);
  d(c[a + 2]);
}
//a:short  b:short  c:int  d:void  
for (; a <= 4; a++)
{
  short *f = &b;
  c[a] || (*f = 0);
  d(c[a + 2]);
}
