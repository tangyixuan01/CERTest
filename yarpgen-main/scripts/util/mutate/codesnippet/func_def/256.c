//e:int;c:int;d:int;a:int [6];f:int;g:char;b:int;fn1:func void
int main()
{
  a[0] = 1;
  for (f = 0; f < 9; f++)
  {
    char h = c;
    int i = 1;
    g = (h < 0) ? (h) : (h >> i);
    e = g;
    for (d = 1; d; d = 0)
      ;

  }

  fn1((g >> 8) & 1);
  if (b != 0)
    __builtin_abort();
  return 0;
}

