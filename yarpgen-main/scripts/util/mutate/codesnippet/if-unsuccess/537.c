//h:int;a:int;i:int;fn2:func int;e:int;fn1:func int;d:int;g:int;c:int;f:short
if (!((1 & i) && f))
{
  if (g)
    ;
  for (g = 0; g != 1; g++)
  {
    g || fn1(fn2(), p);
    if (!d)
    {
      e = g || a;
      break;
    }
  }

  for (h = -1; c >= 0; c = h)
    if (g)
    break;

}
