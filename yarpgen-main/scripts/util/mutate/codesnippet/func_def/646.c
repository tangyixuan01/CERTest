//e:char;c:int;d:char;printf:func int;a:int;fn2:func int;f:int;g:int;b:int;h:int *;fn1:func char
int main()
{
  long m = 249;
  while (c--)
  {
    if ((d < 1) & fn2(2))
      f++;
    if (g)
      break;
    if (f)
      b = --m;
  }

  *h = fn1(m, 6) ^ e;
  printf("%d\n", a);
  return 0;
}

