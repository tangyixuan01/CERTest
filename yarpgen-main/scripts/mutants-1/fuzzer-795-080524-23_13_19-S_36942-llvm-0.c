#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-for-snippet

int printf(const char *, ...);
int a;
int b;
int c;
int d;
int e;
int f;
char g(int h, int i)
{
  return (i > 1) ? (h) : (h >> i);
}

void j()
{
  int IERo03zY = -2147483648;
  for (; b < 1; b++)
  {
    int k = 54;
    for (e = 0; e < 7; e++)
    {
      for (; a < 2; a++)
      {
        ;
      }

    }

    for (d = 0; d < 1; d++)
    {
      if (g(3, k))
      {
        return;
      }
    }

    transparent_crc(k, "k", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  }

  transparent_crc(IERo03zY, "IERo03zY", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  for (IERo03zY = 1; IERo03zY; IERo03zY = ((char) IERo03zY) + 1)
    ;

  f--;
}

int main()
{
  platform_main_begin();
  crc32_gentab();
  j();
  printf("%d\n", f);
  return 0;
}

