#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-assignment-snippet

int printf(const char *, ...);
int a;
int h;
volatile int c;
short b;
short g[2];
char d;
char e;
long f;
int fn1(unsigned int p)
{
  int i;
  for (i = 0; i < 1; i++)
  {
    for (e = 1; e; e++)
    {
      d = p % 3;
      c;
    }

  }

  return p;
}

int main()
{
  platform_main_begin();
  crc32_gentab();
  char f2c_WMdi = (signed char)-128;
  char xR26ywDW = (signed char)-112;
  for (h = 0; h < 2; h++)
  {
    f2c_WMdi = (xR26ywDW == 0) ? (1) : (4 % xR26ywDW);
    g[h] = -8;
  }

  f = a && 1;
  b = fn1(g[0] ^ f);
  printf("%d\n", d);
  transparent_crc(xR26ywDW, "xR26ywDW", 1);
  transparent_crc(f2c_WMdi, "f2c_WMdi", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  return 0;
}

