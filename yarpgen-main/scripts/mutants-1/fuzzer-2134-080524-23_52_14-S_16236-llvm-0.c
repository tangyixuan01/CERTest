#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-if-snippet

int printf(const char *, ...);
union 
{
  short f0;
  int f1;
} u;
union 
{
  short f0;
  int f1;
} *up = &u;
int a;
int main()
{
  platform_main_begin();
  crc32_gentab();
  int rheRBLsy = -2019131916;
  if ((rheRBLsy >= 0) && (rheRBLsy > 0))
    __builtin_printf("0\n");
  for (; a <= 0; a++)
  {
    if (u.f0 = 1)
    {
      up->f1 = 0;
    }
  }

  printf("%d\n", u.f1);
  transparent_crc(rheRBLsy, "rheRBLsy", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  return 0;
}

