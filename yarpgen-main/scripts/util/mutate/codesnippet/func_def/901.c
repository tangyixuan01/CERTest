//e:char;c:short;d:short;a:int;fn2:func char;f:char;b:long long;fn1:func char
void fn3()
{
  f = fn1(b, b);
  short g;
  unsigned long long h = b;
  int j;
  int k = fn2(f - ((d = (((g = h > 1) <= 7) != h) & c) < 40), 2);
  int l = fn2(k, 7);
  int i = 0;
  for (; i < 2; i++)
    j = (g && (g - a)) ? (g) : (g + a);

  e = l & j;
  if (e)
    for (;;)
    ;

}

