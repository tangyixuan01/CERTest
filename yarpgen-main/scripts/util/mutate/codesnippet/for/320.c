//h:char;d:short;f:int;k:int;e:char;j:int;c:int;n:int;b:long;g:int;l:int;i:long;a:long
for (; j < 10; j++)
{
  for (h = 2; h >= 0; h--)
    while (1)
  {
    if (l)
      f = 2;
    int q = (((9L / g) / (~e)) | (n ^ c)) | f;
    int t = 9 & (-(((~e) / q) & g));
    char aa = (((~(((~i) * (-c)) & (~f))) / n) ^ t) ^ 9;
    long ab;
    if (d)
      break;
    if (e)
      ab = (i = (~(((e & (9L * (g * aa))) & (~c)) & f)) & n);
    e = (~(((n / (-i)) * e) & ab)) | (-((-aa) | g));
    i = (((~i) / (~e)) / q) ^ (~t);
    if (g)
    {
      printf("%d\n", e);
    }
    while (h > b)
      ;

    g = (c = e);
    break;
  }


  for (n = 0; n < 10; n++)
    if (a && (h < c))
    break;

  if (k)
  {
    printf("%d\n", k);
    continue;
  }
  f = h % ((unsigned) 9);
}
