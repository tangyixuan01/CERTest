#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-if-snippet -> insert-for-snippet

int printf(const char *, ...);
short a;
short b;
static int c;
static int d;
static int e;
static int f = -7;
char g;
int *h = &d;
int **i = &h;
int j;
short k(int l)
{
  return (l >= 2) ? (0) : (l);
}

int m(int l, int n)
{
  int vRbB_UPZ = 425245200;
  int wQU_ateG = -2147483648;
  transparent_crc(wQU_ateG, "wQU_ateG", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  for (wQU_ateG = 0; wQU_ateG < 16; wQU_ateG++)
    vRbB_UPZ ^= -1;

  transparent_crc(vRbB_UPZ, "vRbB_UPZ", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  return (l < ((-2147483647) / n)) ? (l) : (l * n);
}

void o()
{
  &c;
  if (""['\0'])
  {
    #pragma message "Gotcha!"
    __builtin_abort();
  }
}

int main()
{
  platform_main_begin();
  crc32_gentab();
  {
    int p;
    for (; g <= 3; g++)
    {
      for (; c; c++)
      {
        ;
      }

      a = 2;
      for (; a <= 7; a++)
      {
        short *r = &b;
        p = m(*h, 2022160547);
        unsigned q = 2022160547;
        e = p * q;
        *r ^= e;
        j = k(c + 3);
        *(*i) = 0;
        transparent_crc(q, "q", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
      }

      *i = &f;
    }

  }
  printf("%d\n", b);
}

