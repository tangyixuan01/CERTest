//i:int;e:int;c:int;d:int [1];k:int;j:int;printf:func int;a:int;f:int;g:int;b:int;h:int
int main()
{
  int l;
  int m;
  k = 0;
  for (; g < 1; g++)
  {
    for (i = 0; i < 1; i++)
      m = (b) ? (b) : (1);

    if (m)
      j = (d[e] = 0);
    else
      f = 0;
    l = (k) ? (k) : (a);
    if (d[0] < 1)
    {
      c++;
      h = (l || e) > 0;
    }
  }

  printf("%d\n", c);
  return 0;
}

