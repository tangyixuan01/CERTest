//a:long  __builtin_abort:long  
if (!NumSift(&a, &a))
  __builtin_abort();
//an:int  u:char  x:int  as:short  j:int  w:short  k:int  r:short  s:short  
if (an)
  for (; an >= 0; an--)
  u = x;

else
{
  short at = an = as;
  if (at <= j)
    w = (k) ? (r) : (k);
  if (w)
    return s;
}
