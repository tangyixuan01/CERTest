//h:char;d:int;f:char;g:int;i:int;a:char;j:char;o:int;p:int;e:int;c:int;n:char;b:int;m:int;p2:int
for (; p < 5; p++)
{
  e = 5 | j;
  h = (g = f);
  int q = j && (5 ^ o);
  a = q;
  if (q || o)
  {
    int r = m = ((q % d) || m) || (q % r);
    d = b | c;
    if (i && p2)
      while (n)
      ;

  }
}
