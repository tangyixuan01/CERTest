//s:short;x:int;r:short;u:char;w:short;j:int;an:int;as:short;k:int
if (an)
  for (; an >= 0; an--)
  u = x;

else
{
  short at = an = as;
  if (at <= j)
    w = (k) ? (r) : (k);
  if (w)
    return s;
}
