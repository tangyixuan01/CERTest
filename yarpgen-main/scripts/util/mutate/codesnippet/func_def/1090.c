//i:int;o:short;c:struct S0
{
  int f1;
  int f8;
};d:short;n:char;printf:func int;a:unsigned;h:int;fn1:func unsigned;p:int
static short fn2(struct S0 p1)
{
  int *q = 0;
  int r = 7;
  if (!a)
  {
    c.f1 = 1;
    for (; c.f1; c.f1--)
      if (fn1(10 != 0, p1.f8))
    {
      short s = 9 << ((~o) % (d ^ n));
      int t = s >> (h % (~d));
      p = r;
      r = (s | (p * (d && 9))) | t;
      int u = i & c.f1;
      unsigned v = ~((~(u & h)) | ((~(8 >> c.f1)) & i));
      int w = v;
      if (u < 9)
        w = c.f1;
      if ((i > h) && u)
      {
        printf("%d\n", c.f1);
        continue;
      }
      c.f1 = w;
      if (!p)
        continue;
      return 0;
    }

    for (;;)
      *q = 0;

  }
  return 0;
}

