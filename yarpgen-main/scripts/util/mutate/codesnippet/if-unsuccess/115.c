//h:int;r:int *;a:int;d:int **;c:int *;s:unsigned;j:int;l:short;t:char;m:int;b:int;g:int;p:int
if (g)
{
  int u;
  int **v = &c;
  *d = &b;
  *(*d) = h == p1;
  for (; p1 < 6; p1++)
    for (t = 0; t < 5; t += 4)
  {
    m = 0;
    if ((*(*v)) ? ((~0) % (*(*v))) : (~0))
    {
      if (l)
        *v = &p;
    }
    else
      return;
    u = 0;
    for (; u < 1; u++)
      r = &b;

    *v = r;
    s = (j) ? (j) : (a);
    if (((*(*v)) % s) & 10)
      return;
  }


  *c = 4;
}
