#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-assignment-snippet

int a;
int b;
int c;
int d;
int f;
int g;
int h;
int i;
unsigned int e;
int fn1()
{
  short j = 0;
  for (; j != 8; ++j)
  {
    if (b)
    {
      return 0;
    }
    g = 0;
    short k = b;
    f = (k == 0) ? (0) : (e / k);
    h = ((f + j) < 0) ? (0) : (1);
    c = 0;
    if (1 != b)
    {
      return 0;
    }
    if (j)
    {
      i = (a) ? (0) : (1 % a);
    }
    transparent_crc(k, "k", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  }

  return 1;
}

int main()
{
  platform_main_begin();
  crc32_gentab();
  int JkDI2EVM = 1479806247;
  int H2FRj6qB = -843240157;
  for (; d != (-6); d--)
  {
    fn1();
  }

  transparent_crc(H2FRj6qB, "H2FRj6qB", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  H2FRj6qB = JkDI2EVM < (1 << 30);
  transparent_crc(JkDI2EVM, "JkDI2EVM", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  return 0;
}

