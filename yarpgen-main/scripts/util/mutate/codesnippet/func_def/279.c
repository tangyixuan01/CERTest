//c:int *;d:int **;n:int;j:int;a:int;m:int;b:int;h:int;g:int;l:short;f:int;p:int
static void fn1(int p1)
{
  char t;
  int *r;
  unsigned s;
  if (g)
  {
    int u;
    int **v = &c;
    *d = &b;
    *(*d) = h == p1;
    for (; p1 < 6; p1++)
      for (t = 0; t < 5; t += 4)
    {
      m = 0;
      if ((*(*v)) ? ((~0) % (*(*v))) : (~0))
      {
        if (l)
          *v = &p;
      }
      else
        return;
      u = 0;
      for (; u < 1; u++)
        r = &b;

      *v = r;
      s = (j) ? (j) : (a);
      if (((*(*v)) % s) & 10)
        return;
    }


    *c = 4;
  }
  n = b;
  f = 0;
}

