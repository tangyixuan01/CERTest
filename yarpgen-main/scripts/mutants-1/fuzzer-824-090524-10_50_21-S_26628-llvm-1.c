#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-while-snippet -> insert-switch-snippet

volatile int a;
volatile int b;
volatile int c = 2;
unsigned d;
int main()
{
  platform_main_begin();
  crc32_gentab();
  int * _9AFuixT = &c;
  unsigned char Qspgb5Dt = (unsigned char)255;
  int lehsSG_k = -1950723364;
  int e = -32;
  d = -31;
  for (; d > 2; d++)
  {
    for (e++;; b--)
    {
      if (c)
      {
        break;
        while (lehsSG_k > 1)
          ;

        switch (Qspgb5Dt)
        {
          case 0x95:
            *_9AFuixT = 0x01;
            break;

          case 0x80:

          case 0x81:

          case 0x82:
            *_9AFuixT = 0x00;
            break;

          default:
            *_9AFuixT = 0x05;
            break;

        }

      }
    }

  }

  e && a;
  transparent_crc(lehsSG_k, "lehsSG_k", 1);
  transparent_crc(Qspgb5Dt, "Qspgb5Dt", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  return 0;
}

