//h:int;r:int;o:short;a:unsigned;i:int;n:char;fn1:func unsigned;d:short;c:struct S0
{
  int f1;
  int f8;
};q:int *;printf:func int;p:int
if (!a)
{
  c.f1 = 1;
  for (; c.f1; c.f1--)
    if (fn1(10 != 0, p1.f8))
  {
    short s = 9 << ((~o) % (d ^ n));
    int t = s >> (h % (~d));
    p = r;
    r = (s | (p * (d && 9))) | t;
    int u = i & c.f1;
    unsigned v = ~((~(u & h)) | ((~(8 >> c.f1)) & i));
    int w = v;
    if (u < 9)
      w = c.f1;
    if ((i > h) && u)
    {
      printf("%d\n", c.f1);
      continue;
    }
    c.f1 = w;
    if (!p)
      continue;
    return 0;
  }

  for (;;)
    *q = 0;

}
