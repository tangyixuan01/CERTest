#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-switch-snippet -> insert-switch-snippet

int printf(const char *, ...);
int a;
int b = 1;
int *c = &a;
void foo(int p)
{
  int t;
  for (t = 0; t < 16; t++)
  {
    p ^= -1;
  }

  *c = ~(p & b);
}

int main()
{
  platform_main_begin();
  crc32_gentab();
  int * QbN9tz0F = &b;
  unsigned char jnCR_uQl = (unsigned char)131;
  int V_ojW1ZM = -331344070;
  foo(0);
  switch (jnCR_uQl)
  {
    case 0x95:
      *QbN9tz0F = 0x01;
      break;

    case 0x80:

    case 0x81:

    case 0x82:
      *QbN9tz0F = 0x00;
      break;

    default:
      *QbN9tz0F = 0x05;
      break;

  }

  printf("%d\n", a);
  transparent_crc(V_ojW1ZM, "V_ojW1ZM", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  switch (V_ojW1ZM)
    case 111:
    case 222:
    case 44:
    return 0;




  transparent_crc(jnCR_uQl, "jnCR_uQl", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  return 0;
}

