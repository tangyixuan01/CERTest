//h:int;d:int [1];f:int;a:int;k:int;e:int;c:int;b:int;g:int;m:int;l:int;i:int;j:int
for (; g < 1; g++)
{
  for (i = 0; i < 1; i++)
    m = (b) ? (b) : (1);

  if (m)
    j = (d[e] = 0);
  else
    f = 0;
  l = (k) ? (k) : (a);
  if (d[0] < 1)
  {
    c++;
    h = (l || e) > 0;
  }
}
