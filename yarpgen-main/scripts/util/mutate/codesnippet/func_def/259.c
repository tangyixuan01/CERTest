//e:int;c:struct 
{
  char x;
  int y;
} **;d:struct 
{
  char x;
  int y;
};a:unsigned int;f:int;g:int;b:int
int main()
{
  d.x = 1;
  for (; e <= 0; e++)
    if (!d.x)
  {
    *c = 0;
    g = (b) ? (0) : (a / b);
    f = g * e;
  }

  return 0;
}

