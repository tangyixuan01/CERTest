//e:char;i:int;l:volatile short;c:int;d:int;g:volatile short;j:short;k:int;a:volatile short;m:int;f:short;h:int;b:char
static short fn1()
{
  unsigned short n = 4;
  for (; b >= 0; b--)
    ;

  for (;;)
  {
    int o = ~((e && f) && j);
    int p = ~((~((h % (~k)) ^ e)) & b);
    char q = (~(b | e)) ^ ((p && (o ^ j)) >> (~i));
    short r = ((n & h) && q) | (~(o | (e | p)));
    short s = (((h && q) && j) && (i % l)) * e;
    if (e)
      k = r | b;
    j = s;
    if (!(c >= 0))
    {
      h++;
      printf("g_5326=%lld\n", (long long) 2);
    }
    (a / b) && a;
    short t = ~g;
    n = t;
    L1:
    if (l)
    {
      printf("l_5906=%lld\n", (long long) l);
      goto L2;
    }

    continue;
    L2:
    if (d)
      goto L1;

    return m;
  }

}

