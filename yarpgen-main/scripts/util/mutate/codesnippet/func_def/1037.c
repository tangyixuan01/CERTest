//e:int;c:int;d:int;a:int;f:int;g:int;b:unsigned int
int main()
{
  for (; c < 1; c++)
    for (; e; e++)
  {
    f = (b) ? (1) : (a / b);
    g = f * e;
    if (g)
      for (;; d++)
      ;

  }


  return 0;
}

