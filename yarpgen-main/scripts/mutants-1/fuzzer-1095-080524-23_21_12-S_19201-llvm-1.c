#include "/home/yixuan/csmith/runtime/csmith.h"

// insert-switch-snippet -> insert-while-snippet

int a;
int b = 1;
int c;
int fn1(unsigned char p1, int p2)
{
  return (p2 || (p1 > 1)) ? (0) : (p2);
}

int main()
{
  platform_main_begin();
  crc32_gentab();
  int * ImvVxS1J = &b;
  int * fSkiPaw1 = &b;
  unsigned char htagZReN = (unsigned char)154;
  switch (htagZReN)
  {
    case 0x95:
      *fSkiPaw1 = htagZReN - 0x91;
      break;

    case 0x80:

    case 0x81:

    case 0x82:
      *fSkiPaw1 = htagZReN - 0x80;
      break;

    default:
      *fSkiPaw1 = 0;
      break;

  }

  int d = 0;
  for (; a < 1; a++)
  {
    c = 1;
    while (*ImvVxS1J)
      ;

    fn1((b &= c) | 10L, d);
  }

  transparent_crc(htagZReN, "htagZReN", 1);
  platform_main_end(crc32_context ^ 0xFFFFFFFFUL, 0);
  return 0;
}

