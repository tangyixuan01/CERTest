//e:int;c:int;d:long long;b:int
static int *fn1(long long p)
{
  int g;
  if (d < 0)
    for (b = 1; b >= 0; b--)
  {
    int h = p;
    g = (h == 0) ? (0) : (2 % h);
    e = g || c;
  }

  return 0;
}

