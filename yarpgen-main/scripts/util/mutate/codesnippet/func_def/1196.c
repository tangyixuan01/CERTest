//e:int;c:int;d:int;a:short;f:int;b:int
void fn1(short *p1)
{
  b = (4 % a) & (a / b);
  d = f && (1 / d);
  a = f | (a ^ 1);
  if (a || a)
    printf("l_282=%lld\n", (long long) f);
  printf("g_9=%lld\n", (long long) a);
  d = a;
  *p1 = e;
  if (a && (f < 4))
    c--;
}

