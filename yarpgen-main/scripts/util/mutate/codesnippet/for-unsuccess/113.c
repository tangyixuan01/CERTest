//r:int *;a:int;p:int;v:int **;b:int;m:int;u:int;s:unsigned;t:char;l:short;j:int
for (; p1 < 6; p1++)
  for (t = 0; t < 5; t += 4)
{
  m = 0;
  if ((*(*v)) ? ((~0) % (*(*v))) : (~0))
  {
    if (l)
      *v = &p;
  }
  else
    return;
  u = 0;
  for (; u < 1; u++)
    r = &b;

  *v = r;
  s = (j) ? (j) : (a);
  if (((*(*v)) % s) & 10)
    return;
}

